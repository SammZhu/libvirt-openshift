---
# ansible-galaxy collection install -r requirements.yml
collections:
  - name: ansible.utils        # ipaddr filter (VIP-in-subnet checks in 00-preflight)
    version: ">=2.0.0"
  - name: community.general    # libvirt/virt modules for later phases
    version: ">=8.0.0"
  - name: community.libvirt    # virt, virt_net, virt_pool (Phase 10/40)
    version: ">=1.3.0"
