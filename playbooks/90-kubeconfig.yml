---
# Phase 90 — download kubeconfig + kubeadmin password  (TODO: implement — see README phase table)
# GET kubeconfig + kubeadmin-password into {{ output_dir }}; print console URL and oc login hint.
- name: Phase 90 — download kubeconfig + kubeadmin password
  hosts: local
  gather_facts: false
  tasks:
    - name: Not yet implemented
      ansible.builtin.debug:
        msg: "90-kubeconfig.yml is a stub — GET kubeconfig + kubeadmin-password into {{ output_dir }}; print console URL and oc login hint."
